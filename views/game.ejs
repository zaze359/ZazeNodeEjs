<!DOCTYPE html>
<html lang="en" >

<% var gameList = games %>
//
<table>
    <thead>
    <th>title</th>
    <th>info</th>
    </thead>
    <tbody>
    <% for(var i = 0;i < gameList.length;i++){var u = gameList[i]; %>
    <tr>
        <td><%= u.showcaseTitle %></td>
        <td><%= u.showcaseInfo %></td>
    </tr>
    <% } %>

    <% gameList.forEach(function (t) { %>
        <tr>
            <td><%= t.showcaseTitle %></td>
            <td><%= t.showcaseInfo %></td>
        </tr>
    <% }) %>

    </tbody>

</table>
<% var showcaseList = gameList %>
<% include defalut.ejs %>
<script>
</script>
</html>